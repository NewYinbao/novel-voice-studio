# 更新记录

本项目按语义化版本记录对用户可见的功能、修复和兼容性变化。

## [Unreleased]

### 新增

- 多人拆分片段默认自动去除首尾静音与句内长空白，历史 Session 支持一键清理，克隆导出也按去静音后的实际声音校验。
- 每段新增真实波形音轨，支持拖动/精确秒数裁剪、选区试听和恢复原片段；裁剪后须核对参考台词再导出音色。
- 音轨支持全局和逐段显示/隐藏，单段选择按 Session 隔离；折叠暂停对应试听，不丢失台词草稿或裁剪选区。
- 制作台新增规则一键生成，并将 Codex 与本地 Ollama 合并到统一剧本协作室；规则、协作和导入结果都会形成可恢复的 Session 版本，避免后续生成永久覆盖旧稿。
- 制作台新增可折叠的项目角色音色绑定侧栏，支持仅看当前章节角色、批量草稿与原子保存。
- Codex 剧本协作室支持持久 session、多轮提示词微调和逐轮模型选择。
- Codex 剧本协作室改为独立固定页面，路由包含项目和章节，支持刷新恢复、浏览器前进后退以及一键返回制作台。
- Codex 协作室改为可拖拽三栏工作区；左侧按 Session 展示跨章节版本线，中间采用轻量对话与运行记录，右侧宽度比例可持久化调整；新 Session 会保存可恢复的独立剧本版本快照。
- Codex 剧本任务支持后台执行、可选实时进度摘要、SSE 断线回放，以及关闭窗口或刷新页面后的状态恢复。
- Codex 后台任务可显式开启流式推理摘要与活动日志；摘要经过脱敏和限流，原始思维链、最终剧本 JSON、命令参数与本机路径不会进入日志。
- Codex 新会话默认显式使用 GPT-5.6 Terra + medium，并可在协作室逐轮选择 low、medium、high、xhigh 或 max 推理强度；任务卡和会话历史会记录实际配置。
- Codex 单轮超时可在 5–120 分钟间设置，默认 10 分钟；所选时限会随会话保存，并显示在后台任务进度中。
- 在协作窗口内直接修改台词角色、情绪、强度、语速与停顿，人工改动会带入下一轮 Codex 续问。
- 未登录时可从 Codex 剧本协作室发起官方浏览器登录，并在本地界面查看等待、成功、失败或取消状态。
- 修复 Windows 上 Codex CLI 未自动打开浏览器时登录界面一直等待的问题；现在会安全打开经过验证的官方临时授权地址，并提供手动入口。

### 修复

- 音频编辑采用不可变副本及版本冲突检查，保留原音频，阻止越界编辑目录，并避免旧裁剪请求覆盖新结果。
- Windows 默认命令命中受限的 WindowsApps 副本时，会继续自动发现 LocalAppData 中可执行的 Codex CLI，并区分未登录、受阻和缺失状态。
- Codex 直连改为隔离工作目录、环境白名单和无工具模式；并加入章节版本校验，避免慢请求覆盖处理中产生的手工修改。
- Codex 的可配置子进程上限现在区分“尚未开始响应”和“已生成但未完成”，超时会终止进程、保留当前剧本并释放章节锁供重试。

## [0.1.0] - 2026-08-08

### 新增

- 本地小说导入、自动拆章、规则剧本化与 Codex 结构化交接。
- 多角色音色绑定、录音/音频导入、情绪与节奏编辑。
- Qwen3-TTS 与 CosyVoice 工作器适配器，以及单 GPU 任务队列。
- 单句、本章和整书生成，WAV 试听、缓存与导出。
- AISHELL-3 默认测试音色导入与端到端真实语音烟测。

### 修复

- 缺少角色音色时在提交生成任务前给出明确提示。
- 全部或部分片段生成失败时不再显示为成功。
- 加强项目路径、音色引用、剧本数值和导出完整性校验。
