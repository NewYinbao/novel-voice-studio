# 开发与版本管理

## 本地数据边界

小说原文、录音、音色、生成音频、任务记录和模型运行缓存都属于本机数据，不进入 Git：

- `data/` 只提交空目录占位文件 `data/.gitkeep`。
- `.runtime/` 保存当前机器的 micromamba、模型目录和运行缓存。
- 模型权重应放在外部模型目录，或被忽略的 `worker/models/` 中。
- 不要把 `.env`、授权令牌、私人小说或真人声音样本加入提交。

## 开发流程

1. 从稳定分支创建一个用途单一的开发分支。
2. 修改范围尽量小；不要顺手格式化无关文件。
3. 提交前运行完整质量门槛：

   ```powershell
   npm run verify
   ```

4. 真实 TTS 改动还应启动模型工作器，并至少生成一个短句验证媒体接口与试听。
5. 提交信息使用简短的祈使句，说明产生的结果，例如 `fix: report missing character voices`。

## 目录职责

- `public/`：零依赖 Web UI。
- `src/`：Node.js API、存储、剧本与音频流水线。
- `worker/`：隔离的 Python TTS 工作器及模型适配器。
- `schemas/`：结构化剧本契约。
- `scripts/`：环境安装、启动和测试音色导入脚本。
- `tests/`：不依赖外部模型的快速回归测试。

如果改动了 API、持久化格式、模型安装方式或许可证边界，请同步更新 `README.md`。
