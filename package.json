{
  "name": "novel-voice-studio",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "description": "本地优先的多角色小说有声书制作台",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node scripts/check-js.mjs",
    "verify": "npm run check && npm test",
    "seed:voices": "node scripts/seed-default-voices.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
